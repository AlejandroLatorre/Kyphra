"""`kyphra scan <log-dir>` retroactive audit command.

Reads historical Claude Code / Cursor logs, classifies each prompt,
and produces a PDF report with summary + top risky prompts.

This is the "entry wedge" deliverable for the first design partner audits.
"""
